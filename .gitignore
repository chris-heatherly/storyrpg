# macOS
.DS_Store
*.pem

# IDE / Editor
.idea/
*.swp
*.swo
*~

# Cursor agent transcripts + editor settings (local only) — but track shared skills
.cursor/*
!.cursor/skills
**/.cursor/*
!**/.cursor/skills

# Auto-commit log
scripts/.auto-commit.log

# Environment variables / secrets
.env
.env.*
!.env.example

# Firebase Hosting local cache
storyrpg-prototype/.firebase/
.firebase/

# Guard against re-committing the nested-duplicate dir and codex scratch folders
storyrpg-prototype/storyrpg-prototype/
**/.codex-tmp/
# Runtime log from the update-docs skill (local only)
**/.cursor/skills/update-docs/scripts/last-auto-update.log
