interface:
  display_name: "StoryRPG Image Pipeline"
  short_description: "Maintain StoryRPG storyboard and image generation"
  default_prompt: "Use $storyrpg-image-pipeline for StoryRPG storyboard-v2, image providers, style contracts, references, QA, and LoRA."
policy:
  allow_implicit_invocation: true
