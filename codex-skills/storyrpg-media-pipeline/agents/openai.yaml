interface:
  display_name: "StoryRPG Media Pipeline"
  short_description: "Work on StoryRPG image and audio-narration generation"
  default_prompt: "Use $storyrpg-media-pipeline for StoryRPG image generation, providers, style/reference packs, LoRA, and audio narration (ElevenLabs TTS + karaoke alignment)."
policy:
  allow_implicit_invocation: true
