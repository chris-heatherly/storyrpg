interface:
  display_name: "StoryRPG Pipeline Agent Development"
  short_description: "Build and modify StoryRPG generation-pipeline LLM agents"
  default_prompt: "Use $storyrpg-pipeline-agent-development when creating or editing a BaseAgent subclass, LLM prompts, JSON parsing, or converters in the StoryRPG pipeline."
policy:
  allow_implicit_invocation: true
