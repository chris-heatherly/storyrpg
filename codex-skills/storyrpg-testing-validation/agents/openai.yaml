interface:
  display_name: "StoryRPG Testing Validation"
  short_description: "Choose focused StoryRPG validation and boundary checks"
  default_prompt: "Use $storyrpg-testing-validation to choose focused StoryRPG tests, typechecks, reader/generator boundary checks, proxy/DB checks, or Playwright verification."
policy:
  allow_implicit_invocation: true
