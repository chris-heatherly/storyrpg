/**
 * Prompt Constants
 * Common prompt fragments and templates
 */

export const PROMPT_TEMPLATES = {
  fictionFirst: `Write as if the reader is experiencing the story firsthand. Show don't tell.`,
  jsonOnly: `Return ONLY valid JSON with no markdown formatting or extra text.`,
};
